<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | Trace
    </title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;600&display=swap');

        body {
            font-family: 'Montserrat', Arial, sans-serif;
            background-color: #f0f2f5;
            margin: 0;
            padding: 40px 0;
        }

        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 16px;
            overflow: hidden;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: linear-gradient(135deg, #1e3a8a, #3b82f6);
            color: white;
            text-align: center;
            padding: 30px;
        }

        .logo {
            font-size: 32px;
            font-weight: 600;
            letter-spacing: 2px;
            margin-bottom: 10px;
        }

        .header h1 {
            margin: 0;
            font-weight: 400;
            font-size: 24px;
            letter-spacing: 1px;
        }

        .body {
            padding: 40px;
            color: #333;
        }

        .verification-container {
            background-color: #f0f9ff;
            border-radius: 12px;
            padding: 20px;
            margin: 20px 0;
            text-align: center;
            position: relative;
            overflow: hidden;
            border: 1px solid #bfdbfe;
        }

        .verification-container::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(to bottom right,
                    rgba(30, 58, 138, 0.05),
                    rgba(59, 130, 246, 0.05));
            animation: rotate 20s linear infinite;
            z-index: 1;
        }

        .button {
            display: inline-block;
            padding: 12px 24px;
            background-color: #bfd1ee;
            color: white;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            transition: background-color 0.3s ease;
            position: relative;
            z-index: 2;
        }

        .button:hover {
            background-color: #c3cce1;
        }

        .link {
            word-break: break-all;
            color: #3b82f6;
        }

        .footer {
            text-align: center;
            padding: 20px;
            background-color: #f0f9ff;
            color: #64748b;
            font-size: 14px;
        }

        @keyframes rotate {
            0% {
                transform: rotate(0deg);
            }

            100% {
                transform: rotate(360deg);
            }
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="header">
            <div class="logo">TRACE</div>
            <h1><%- title %></h1>
        </div>
        <div class="body">
            <p>Hello <%- userName %>,</p>
            <p>Thank you for signing up with Trace. To complete your registration, please verify your email address:
            </p>
            <div class="verification-container">
                <a href="<%- verificationLink %>" class="button">Verify Email Address</a>
            </div>
            <p>If the button above doesn't work, you can also click the link below or copy and paste it into your
                browser:</p>
            <p><a href="<%- verificationLink %>" class="link"><%- verificationLink %></a></p>
            <p>This verification link will expire in 24 hours. If you didn't create an account with Apollo, please
                ignore this email.</p>
        </div>
        <div class="footer">
            <p>&copy; <%= new Date().getFullYear() %> TRACE. All rights reserved.</p>
        </div>
    </div>
</body>

</html>